# 包含 # 的行都会被测试用例生成器无视

# 一个测试条目的内容如下：
#   enum{"变量名称":可能的范围}
#   （所有枚举类型都必须是整数类型，因此它的声明简单得多）

#   range{"变量名称":变量数值类型:测试强度:可能的范围}
#   （范围类型理论上有无限种可能，无法遍历，因此必须设置 数值类型 和 测试强度）
#    
# enum{"产品类型":1,02,13,}
# enum{"参与人数":43,41,}
# enum{"price":52,26,49,}

# range{"波动范围":double:0:(2,18),}
# range{"工作时间":int:0:[1,2],}
# range{"核心强度":double:0:(-7.6,-1),}
# range{"全体实数的范围":int:0:(MIN,MAX),}
range{"测试一下":double:3:(MIN,MAX),}

# range{"满意度":mix:0:(2,9],(7.4,12),}